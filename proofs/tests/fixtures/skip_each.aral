# Total must be non-negative (compilable)
invariant total_non_negative:
  order.total >= 0

# Each line item must have positive quantity (NOT compilable — uses 'each')
invariant line_items_positive:
  each item in order.lineItems:
    item.quantity > 0
