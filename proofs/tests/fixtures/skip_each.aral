# Total must be non-negative (compilable)
invariant total_non_negative:
  order.total >= 0

# Each line item must have positive quantity (now compilable with each())
invariant line_items_positive:
  each(order.lineItems, quantity > 0)
